#include <stdio.h>

int main()
{
    char line[150];
    int i, j;
    printf("Enter a line of string: ");
    scanf("%s",line);

    for(i = 0; line[i] != '\0'; ++i)
    {}

    printf("Length of string is %d.\n",i);

    return 0;
}
